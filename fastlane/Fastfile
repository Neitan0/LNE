# This file contains the fastlane.tools configuration
# You can find the documentation at https://docs.fastlane.tools
#
# For a list of all available actions, check out
#
#     https://docs.fastlane.tools/actions
#
# For a list of all available plugins, check out
#
#     https://docs.fastlane.tools/plugins/available-plugins
#

# Uncomment the line if you want fastlane to automatically update itself
# update_fastlane

default_platform(:ios)

platform :ios do
desc "Roda testes com relatório de cobertura de código"
  lane :check_build do
  
    output_directory = ENV["BITRISE_DEPLOY_DIR"] || "test_output"

    run_tests(
      workspace: "WebQuiz.xcworkspace",
      scheme: "WebQuiz",
      clean: true,
      code_coverage: true,
      output_directory: output_directory,
      output_types: "html,junit",
      xcargs: "CODE_SIGNING_ALLOWED=NO CODE_SIGNING_IDENTITY='' PROVISIONING_PROFILE=''",
      fail_build: true
    )
  end
end
